export interface ITextConfig {
    title: string
    description: string
    searchButton: string
    departDate: string
    returnDate: string
}

export interface ITexts {
    ru: ITextConfig
    en: ITextConfig
}
